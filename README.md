# Лабораторная работа №6

## 📝 Описание
Финальная работа, объединяющая создание собственных шаблонных контейнеров, использование наследования и стандартов профессионального документирования кода.

## ✅ Структура проекта
1. **UniqueList<T>**: Шаблонный класс неупорядоченного списка, хранящий только уникальные элементы. Реализованы методы: `Union`, `Except`, `Intersect`, `Contains`.
2. **MyDictionary<K, V>**: Собственная реализация словаря (ассоциативного массива) без использования стандартных библиотек `std::map`/`std::unordered_map`.
3. **Points (Наследование)**: 
   - `Point2D` — базовый класс.
   - `Point3D` — производный класс с расчетом расстояния в трехмерном пространстве.

## 📖 Документация
Весь код снабжен **XML-комментариями** (`/// <summary>`), которые позволяют:
- Генерировать документацию через **Doxygen**.
- Видеть подсказки IntelliSense в Visual Studio / VS Code (актуально для 2026 года).

## 🛠 Сборка и запуск
1. Создайте папку для сборки: `mkdir build && cd build`
2. Сконфигурируйте проект: `cmake ..`
3. Соберите проект: `cmake --build .`
4. Запуск: `./Lab3_Executable`

## 📄 Данные
Задание 2 использует файл `phones.txt` (или `data.txt`) для загрузки начальных данных словаря.
